# ===================================================================
# Obsidian 지식 보관함 (회사 데이터 자산 지식화) — 절대 commit 금지
# 이 repo는 PUBLIC 이므로 볼트 전체를 무시한다. (사내 지식·고객·운영 노트 포함)
# ===================================================================
/theplenty-management/
**/.obsidian/

# ===================================================================
# 의존성
# ===================================================================
node_modules/
*/node_modules/
**/node_modules/

# ===================================================================
# 빌드 산출물
# ===================================================================
dist/
build/
*/dist/
*/build/

# ===================================================================
# 환경 변수 — 절대 commit 금지 (실제 API 키, Firebase 키, super admin email 포함)
# ===================================================================
.env
.env.local
.env.*.local
.env.production
.env.development.local
.env.test.local

# ===================================================================
# 사용자 데이터 — 회사 자산. 절대 commit 금지.
# ===================================================================
# JSON DB 본체
server/data/*.json
# atomic-write 임시 파일
server/data/*.json.tmp.*
# 이전 버전 롤링 백업
server/data/*.json.prev
# 타임스탬프 백업 (.bak.1778045387237 등)
server/data/*.json.bak.*
# 일자별 스냅샷 폴더
server/data/backups/
# 첨부파일 업로드 폴더 (계약서, BEO, 최종 INVOICE 등)
server/data/uploads/
# .gitkeep만 예외로 유지하여 폴더 구조는 보존
!server/data/.gitkeep

# ===================================================================
# Firebase / 서비스 계정 인증 파일
# ===================================================================
참고/
*-firebase-adminsdk-*.json
*serviceAccount*.json
google-credentials*.json

# ===================================================================
# 로컬 자격증명/설정 메모 — 토큰·시크릿 평문 포함. Public repo 절대 금지.
# (예: Slack 앱 Client/Signing Secret, Bot/App 토큰)
# ===================================================================
docs/sales-agent.txt

# ===================================================================
# 웨딩 랜딩 미디어 원본 (영상 2GB+·행사 실사진) — Storage로 서빙, repo 금지
# ===================================================================
/landing-assets/

# ===================================================================
# 문의 트래커 이관 자료 — 고객 연락처·상담 메모 원본. Public repo 절대 금지.
# 우리 프로그램으로 흡수한 뒤에는 이 폴더 자체를 지울 것.
# ===================================================================
/plenty-tracker-이사-폴더/

# ===================================================================
# 엑셀 / CSV — 고객·행사 정보 export 파일이 루트에 떨어질 수 있음
# ===================================================================
*.xlsx
*.xls
*.csv

# ===================================================================
# 압축 백업 (옵시디언 볼트 등) + 옵시디언 잡파일 — 절대 commit 금지
# 2026-07-29: vault_backup zip이 커밋됐다가 히스토리 정리한 사고 재발 방지
# ===================================================================
*.zip
*.canvas
*.base

# ===================================================================
# 매출 마이그레이션 소스 데이터 — 내부 행사/계약금액 포함, 절대 commit 금지
# ===================================================================
server/src/store/revenueExcel2026Data.json

# ===================================================================
# IDE / OS / 임시
# ===================================================================
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ===================================================================
# 로그 / 캐시
# ===================================================================
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cache/

# ===================================================================
# Claude Code 로컬 작업 폴더 — 개인 PC 종속 설정 + 워크트리 사본. Public repo 금지.
# worktrees/ 는 저장소 '전체 사본'이라 실수로 add 하면 통째로 공개된다.
# 2026-08-05: 하위 파일 단위로만 막혀 있어 worktrees/ 와 백업본이 새던 것을 폴더 통째로 차단.
# 나중에 특정 파일(예: 팀 공용 설정)을 공유하려면 아래처럼 예외를 추가할 것:
#   !.claude/settings.json
# ===================================================================
.claude/*
.claude/projects/

# ===================================================================
# 임시 폴더
# ===================================================================
tmp/
temp/
.tmp/

# ===================================================================
# Firebase CLI 캐시 / 배포 산출물
# ===================================================================
.firebase/
firebase-debug.log
firebase-debug.*.log

# ===================================================================
# TypeScript incremental build cache
# ===================================================================
*.tsbuildinfo

# ===================================================================
# Hermes AI 에이전트 (슬랙봇) — 별도 git repo + API 키 포함
# Public repo에 절대 commit 금지.
# ===================================================================
/plenty-agent/

# ===================================================================
# IR 캡처 이미지 — 앱 화면 캡처(고객명/실데이터 포함). Public repo 금지.
# ===================================================================
/IR_캐프처/
